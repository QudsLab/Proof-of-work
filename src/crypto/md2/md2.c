/*
 * Standalone MD2 implementation
 * Based on RFC 1319 - The MD2 Message-Digest Algorithm
 * No OpenSSL dependencies
 */

#include "md2.h"
#include <string.h>

/* MD2 S-box (permutation of 0..255) */
static const uint8_t S[256] = {
    
};

/* MD2 block transformation */
static void md2_transform(MD2_CTX *ctx, const uint8_t *block) {
    uint8_t temp[48];
    uint8_t t;
    int i, j;
    
    /* Form encryption block from state, block, state XOR block */
    for (i = 0; i < 16; i++) {
        temp[i] = ctx->state[i];
        temp[i + 16] = block[i];
        temp[i + 32] = ctx->state[i] ^ block[i];
    }
    
    /* Encrypt block (18 rounds) */
    t = 0;
    for (i = 0; i < 18; i++) {
        for (j = 0; j < 48; j++) {
            t = temp[j] ^= S[t];
        }
        t = (t + i) & 0xFF;
    }
    
    /* Save state */
    memcpy(ctx->state, temp, 16);
    
    /* Update checksum */
    t = ctx->checksum[15];
    for (i = 0; i < 16; i++) {
        t = ctx->checksum[i] ^= S[block[i] ^ t];
    }
}

/* Initialize MD2 context */
void md2_init(MD2_CTX *ctx) {
    memset(ctx->state, 0, 16);
    memset(ctx->checksum, 0, 16);
    ctx->count = 0;
}

/* Update MD2 context with input data */
void md2_update(MD2_CTX *ctx, const uint8_t *data, size_t len) {
    
    if (len == 0) return;
    
    /* Process buffered data if any */
    if (ctx->count > 0) {
        size_t space = 16 - ctx->count;
        if (len < space) {
            memcpy(&ctx->buffer[ctx->count], data, len);
            ctx->count += len;
            return;
        }
        memcpy(&ctx->buffer[ctx->count], data, space);
        md2_transform(ctx, ctx->buffer);
        data += space;
        len -= space;
        ctx->count = 0;
    }
    
    /* Process complete blocks */
    while (len >= 16) {
        md2_transform(ctx, data);
        data += 16;
        len -= 16;
    }
    
    /* Buffer remaining data */
    if (len > 0) {
        memcpy(ctx->buffer, data, len);
        ctx->count = len;
    }
}

/* Finalize MD2 hash and produce digest */
void md2_final(uint8_t digest[MD2_DIGEST_LENGTH], MD2_CTX *ctx) {
    uint8_t padding[16];
    size_t padLen = 16 - ctx->count;
    
    /* Pad to 16 bytes */
    memset(padding, padLen, padLen);
    md2_update(ctx, padding, padLen);
    
    /* Append checksum */
    md2_transform(ctx, ctx->checksum);
    
    /* Output digest */
    memcpy(digest, ctx->state, 16);
    
    /* Clear sensitive information */
    memset(ctx, 0, sizeof(*ctx));
}

/* Convenience function: compute MD2 hash in one call */
void md2_hash(const uint8_t *data, size_t len, uint8_t digest[MD2_DIGEST_LENGTH]) {
    MD2_CTX ctx;
    md2_init(&ctx);
    md2_update(&ctx, data, len);
    md2_final(digest, &ctx);
}
